@isTest
public class CoordinateTriggerTest {
    @isTest static void singlePosTest(){
        //add new coordinate and related Account and Starport Navigator
        Account accTest = new Account(Name = 'AccTest', Credits__c = 20.21);
        insert accTest;
        Starport_Navigator__c navTest = new Starport_Navigator__c(Name = 'NavTest');
        insert navTest;
        Coordinate__c coorTest = new Coordinate__c(Name = 'CoorTest', Account__c = accTest.Id, Starport_Navigator__c = navTest.Id, Sector__c = 20);
        insert coorTest;
        //now testing the update
        coorTest.Sector__c = 35;
        update coorTest;
    	//now checking if Price equals what it is supposed to, which
    	//for this case (sector = 35), price should be 400
    	System.assertEquals(400, coorTest.Price__c);
    }
}